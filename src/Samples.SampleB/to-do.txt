- Change icon name

Databases:
	- Clone Table, Field, Queries, Clauses
	- Bug Delete. Pas de "Delete table"
	- Bug Insert. Pas de schema dans le nom de la table.
	- isParamaterInjected -> ParameterMode = Symboled, Scripted, ValueInjected
	- Bug in Connection.CreateCommandText.
	- Bug in query building with Select when there is no From.
	- Bug in PgSqlServer query building: limit in select
	- Bug Update with one From and Table defined in query.
	- Bug in query build when returning fields.



- Loading usable configuration: ok

Bug fixes:
- Json serialization when multiple xml element attributes are defined for the same property.

Features to add and test:

- Integrate .net core logging
- Integrate .net core configuration

- Check Json serialization of data elements

- Simplify settings loading: extract from bot initialization to settings UpdateFromFile function.

- Simplify logs : lighten logs, define activity logs

- Data items : remove Specified suffixes
- Defining default settings and simplifying for bot (optionnally)
	AddBindOpenService : remove loggers, predicate loggers
- Loading extension items: script word index loading, entities, tasks.
- Handling tasks.
- Handling references.
- Handling application options (with sub sets).
- Loading external assemblies.
- Data source as element.
- Load generic data item like dataelement.

- Erasing old log files.